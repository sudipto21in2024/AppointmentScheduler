version: "3.9"
services:
  userservice:
    build: ./backend/services/UserService
    ports:
      - "5000:80"
    environment:
      - ASPNETCORE_ENVIRONMENT=Development

  # Add other services here (e.g., BookingService, PaymentService)